localdns
----

Passively listens for DHCP leases on the local network and automatically serves the hostname to IP mapping over DNS.

Usage:

    go mod init localdns; go get; go build
    (sudo) ./localdns <interface>
